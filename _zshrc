export ZSH=$HOME/.oh-my-zsh
ZSH_THEME="robbyrussell"

# Time stamp shown in the history command output.
HIST_STAMPS="yyyy-mm-dd"

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Example format: plugins=(rails git textmate ruby lighthouse)
plugins=(git)

# User configuration

export PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/git/bin:/usr/texbin:/opt/local/bin:/opt/local/sbin:/usr/local:/usr/local/Cellar/eigen/3.2.5/include/eigen3:/usr/local/include:/usr/local/texlive/2015/bin/x86_64-darwin:/Users/ibayashi/shell_scripts:/usr/local/texlive/2021/bin/universal-darwin"
# export MANPATH="/usr/local/man:$MANPATH"
source $ZSH/oh-my-zsh.sh

# Language environment
export LANG=en_US.UTF-8

# ssh
 export SSH_KEY_PATH="~/.ssh/id_rsa"

# Export
export EDITOR='vim'
export PATH=$PATH:~/bin:/home/hikaru/Downloads/node-v8.5.0-linux-x64/bin:/home/hikaru/Downloads/otfcc/bin/release-x64

# Vim keybind
bindkey -v
function zle-line-init zle-keymap-select {
    VIM_NORMAL="%K{208}%F{black}%k%f%K{208}%F{white} % NORMAL %k%f%K{black}%F{208}%k%f"
    VIM_INSERT="%K{075}%F{black}%k%f%K{075}%F{white} % INSERT %k%f%K{black}%F{075}%k%f"
    RPS1="${${KEYMAP/vicmd/$VIM_NORMAL}/(main|viins)/$VIM_INSERT}"
    RPS2=$RPS1
    zle reset-prompt
}
zle -N zle-line-init
zle -N zle-keymap-select

# Set personal aliases
alias m="make"
alias mc="make clean"
alias o="open"
alias vps="globalprotect connect --portal mdr-vpn.ads.isi.edu"  
alias vpsd="globalprotect disconnect"
# alias brew="arch -x86_64 brew"
source $HOME/configs/myShellConfig.sh
# export PATH="/Users/hikaruibayashi/anaconda3/bin:$PATH"  # commented out by conda initialize 
export PATH="/usr/local/opt/python@3.9/bin:$PATH" 
export PATH="/Applications/Postgres.app/Contents/Versions/latest/bin:$PATH" 

# opam configuration
test -r /Users/hibayashi/.opam/opam-init/init.zsh && . /Users/hibayashi/.opam/opam-init/init.zsh > /dev/null 2> /dev/null || true
# arch -x86_64 brew install


# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/Users/hikaruibayashi/anaconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/Users/hikaruibayashi/anaconda3/etc/profile.d/conda.sh" ]; then
        . "/Users/hikaruibayashi/anaconda3/etc/profile.d/conda.sh"
    else
        export PATH="/Users/hikaruibayashi/anaconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<
export PATH="/usr/local/opt/ruby/bin:/usr/local/lib/ruby/gems/3.0.0/bin:$PATH"
export PATH="$HOME/.gem/ruby/3.0.0/bin:$PATH"
export PATH="$PATH:/Applications/Visual Studio Code.app/Contents/Resources/app/bin"
export PATH="$PATH:$HOME/.cargo/bin"
export PATH="/usr/local/bin/nvim:$PATH"
export PATH="$PATH:/Users/hikaruibayashi/chromedriver_mac64"

PATH="/Users/hikaruibayashi/perl5/bin${PATH:+:${PATH}}"; export PATH;
PERL5LIB="/Users/hikaruibayashi/perl5/lib/perl5${PERL5LIB:+:${PERL5LIB}}"; export PERL5LIB;
PERL_LOCAL_LIB_ROOT="/Users/hikaruibayashi/perl5${PERL_LOCAL_LIB_ROOT:+:${PERL_LOCAL_LIB_ROOT}}"; export PERL_LOCAL_LIB_ROOT;
PERL_MB_OPT="--install_base \"/Users/hikaruibayashi/perl5\""; export PERL_MB_OPT;
PERL_MM_OPT="INSTALL_BASE=/Users/hikaruibayashi/perl5"; export PERL_MM_OPT;
alias brew="arch -x86_64 brew"

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/hikaruibayashi/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/hikaruibayashi/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/hikaruibayashi/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/hikaruibayashi/google-cloud-sdk/completion.zsh.inc'; fi

[ -f "/Users/hikaruibayashi/.ghcup/env" ] && source "/Users/hikaruibayashi/.ghcup/env" # ghcup-env
alias python="python3"
alias pip="pip3"
source ~/myenv/bin/activate
export KAGGLE_USERNAME=ibayashihikaru
export KAGGLE_KEY=3bc640a118aa7e4591362765ad374f0e
export PATH="/usr/local/texlive/2024/bin/universal-darwin:$PATH"
dump_zsh_state() {
    # Dump current zsh state for Cursor integration
    echo "ZSH_VERSION=$ZSH_VERSION"
    echo "PWD=$PWD"
    echo "PATH=$PATH"
    echo "SHELL=$SHELL"
}
